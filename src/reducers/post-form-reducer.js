const initialState = {
  image: {
    file: ""
  },
  sport: "",
  tags: [],
  text: ""
};

const postForm = function(state = initialState, action) {
  switch(action.type) {
    default: {
      return state;
    }
  }
}

export default postForm;
